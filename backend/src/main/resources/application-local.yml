spring:
  datasource:
    url: jdbc:h2:mem:orbite;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password: ''
  sql:
    init:
      mode: always
      schema-locations: classpath:schema-local.sql
  h2:
    console:
      enabled: true
      path: /h2-console
